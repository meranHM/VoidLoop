import SectionContainer from "./SectionContainer"


export default function HeroSection() {
    return (
        <SectionContainer
            className="bg-rich-black"
        >
            <h1
                className="text-4xl"
            >
                Hero Section (Video Backround)
            </h1>
        </SectionContainer>
    )
}